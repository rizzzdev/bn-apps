version: '3.8'

services:
  akademik-bn:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - PUBLIC_API_URL=https://api-bn.${DOMAIN_NAME}
        - PUBLIC_PORTAL_URL=https://apps-bn.${DOMAIN_NAME}
        - PUBLIC_COOKIE_DOMAIN=${DOMAIN_NAME:-}
    container_name: akademik-bn-app
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - HOST=0.0.0.0
      - PORT=5173
      # Pass dynamic API URL or other environment variables below
      - PUBLIC_API_URL=https://api-bn.${DOMAIN_NAME}
      - PUBLIC_PORTAL_URL=https://apps-bn.${DOMAIN_NAME}
      - PUBLIC_COOKIE_DOMAIN=${DOMAIN_NAME:-}
      - PUBLIC_ALLOWED_ROLES=${PUBLIC_ALLOWED_ROLES:-super_admin,teacher}
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:5173/_app/version.json"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - proxy
      - backend
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.akademik-bn.rule=Host(`akademik-bn.${DOMAIN_NAME}`)"
      - "traefik.http.routers.akademik-bn.entrypoints=websecure"
      - "traefik.http.routers.akademik-bn.tls.certresolver=myresolver"
      - "traefik.http.services.akademik-bn.loadbalancer.server.port=5173"

networks:
  proxy:
    external: true
  backend:
    external: true
